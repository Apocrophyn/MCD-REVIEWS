# Required for real receipt classification, extraction, and feedback generation.
# When omitted in development, AI actions are disabled instead of returning sample data.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

PORT=8787
DATA_DIR=.data
MAX_UPLOAD_MB=10
MAX_IMAGES_PER_RECEIPT=3
